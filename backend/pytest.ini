[pytest]
DJANGO_SETTINGS_MODULE = config.settings.dev
python_files = test_*.py
testpaths = .
# --reuse-db skips recreating the test database on every run. Docker
# networking on Windows makes schema creation the dominant cost.
# Use --create-db after a migration.
addopts = -q --no-header --reuse-db
